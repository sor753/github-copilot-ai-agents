# コードレビュー: 2026-06-28 08:54

## レビュー対象

- 対象ファイル / コミット範囲: `src/` 全体、直近差分なし
- 実施日: 2026-06-28

---

## レビュー結果

### [src/pages/HomePage.tsx](../../src/pages/HomePage.tsx) / [src/pages/JournalFormPage.tsx](../../src/pages/JournalFormPage.tsx) / [src/features/journal/services/journal-storage.ts](../../src/features/journal/services/journal-storage.ts)

#### ⚠️ 要修正
- **日付計算が UTC かつモジュール読み込み時に固定されており、ローカル日付とずれる**
  - 該当箇所: `const today = new Date().toISOString().slice(0, 10);` と `return date.toISOString().slice(0, 10);`
  - 修正案: ローカル日付の `YYYY-MM-DD` を返す共通 helper に寄せ、`today` も render 時または都度計算する
  - 根拠: SPA を開いたまま日付をまたぐと古い日付のままになり、UTC 変換でもローカルの「今日」と一致しないため、作成・更新・集計がずれる

### [src/features/journal/components/journal-entry-form.tsx](../../src/features/journal/components/journal-entry-form.tsx)

#### ⚠️ 要修正
- **`initialValue` 変更時にローカル state が同期されず、別レコードへ移動しても古い入力が残る**
  - 該当箇所: `const [date, setDate] = useState(initialValue.date);` / `const [mood, setMood] = useState<Mood>(initialValue.mood);` / `const [text, setText] = useState(initialValue.text);`
  - 修正案: `initialValue` の変更で state を再初期化するか、`date` などを `key` にしてフォームを再マウントする
  - 根拠: 編集画面やホーム画面のクイック追加で取得済みデータが更新されたときに、フォーム表示と実データが食い違う

#### 💡 提案
- **`rounded-full` の使用は規約と逆行するので `rounded-md` などへ寄せる**
  - 該当箇所: クイック日付ボタンと気分ボタン
  - 修正案: 角丸を控えめにして、既存のボタン群と統一する

### [src/main.tsx](../../src/main.tsx)

#### ⚠️ 要修正
- **ErrorBoundary がなく、`App` 配下のランタイム例外がそのままクラッシュする**
  - 該当箇所: `createRoot(container).render(...)`
  - 修正案: ルートを ErrorBoundary で包み、少なくとも簡単なリカバリ UI を返す
  - 根拠: ルートパラメータ検証や localStorage パース失敗のような例外が、画面全体の停止に直結する

---

## 総評

`src/` 全体として、構造と責務分離は概ね良好でした。重大な指摘は日付処理の一貫性、フォーム state の同期、ルート境界の例外処理の3点です。

---

## 追記レビュー

### [src/features/journal/services/journal-storage.ts](../../src/features/journal/services/journal-storage.ts)

#### ⚠️ 要修正
- **直近7日集計が時刻の影響で1日欠ける可能性がある**
  - 該当箇所: `countRecentEntries` 内の `from` と `today`
  - 内容: `from` と `today` が現在時刻付きのまま比較される一方、entry 側は `00:00:00` 固定なので、境界日が除外されるケースがある
  - 修正案: `today` / `from` を日付境界で正規化して比較する

#### ⚠️ 要修正
- **型アサーションに理由コメントがない**
  - 該当箇所: `value as Mood` / `value as Record<string, unknown>`
  - 内容: 規約上、`as` を使う場合は安全性の理由コメントが必要
  - 修正案: 型ガードに寄せるか、理由コメントを追加する

### [src/pages/HomePage.tsx](../../src/pages/HomePage.tsx) / [src/pages/JournalFormPage.tsx](../../src/pages/JournalFormPage.tsx)

#### ⚠️ 要修正
- **日付初期値をモジュールロード時に固定しており、日跨ぎで古い日付を参照する**
  - 該当箇所: `const today = new Date().toISOString().slice(0, 10);`
  - 内容: 画面を開いたまま日付が変わると「今日」が更新されない
  - 修正案: コンポーネント内で都度計算するか、日単位で再評価する

- **Mutation エラーの UI ハンドリングが不足している**
  - 該当箇所: `mutateAsync` 呼び出し周辺
  - 内容: 失敗時にユーザー向けの表示がなく、エラーが見えづらい
  - 修正案: `isError` / `error` を表示し、必要なら `try/catch` で明示的に制御する

### [src/features/journal/components/journal-entry-form.tsx](../../src/features/journal/components/journal-entry-form.tsx)

#### ⚠️ 要修正
- **Tailwind 規約違反がある**
  - 該当箇所: クイック日付ボタン、気分ボタン
  - 内容: `rounded-full` の使用と、クラス結合が `cva` / `cn` 方針と不一致
  - 修正案: `rounded-md` ベースに統一し、バリアント管理を `cva` / `cn` に寄せる

- **最小タップ領域 44x44 を満たさない操作要素がある**
  - 該当箇所: クイック日付ボタン、気分選択
  - 内容: `min-h-11` / `min-w-11` が未指定
  - 修正案: インタラクティブ要素に最小サイズを付与する

### [src/shared/components/app-layout.tsx](../../src/shared/components/app-layout.tsx)

#### ⚠️ 要修正
- **ヘッダーナビリンクが最小タップ領域を満たしていない**
  - 該当箇所: `NavLink` のクラス定義
  - 内容: `min-h-11` / `min-w-11` がないため、タップしにくい場面がある
  - 修正案: 最小サイズを付与し、必要ならバリアントを共通化する

### [src/features/journal/hooks/use-journal-queries.ts](../../src/features/journal/hooks/use-journal-queries.ts)

#### ⚠️ 要修正
- **フックの戻り値型が明示されていない**
  - 該当箇所: `useJournalEntries` / `useJournalEntry` / `useJournalSummary`
  - 内容: 規約では関数戻り値の明示が求められる
  - 修正案: `UseQueryResult` / `UseMutationResult` などで戻り値型を付与する

### テストギャップ

- **境界日の集計ロジックを検証するテストがない**
  - 対象: `src/features/journal/services/journal-storage.test.ts`
  - 内容: `recentEntries` の境界日が 1 日ずれるケースを防ぐテストが必要

### 問題なし

- ルーティング定義は `App.tsx` に集約されており構成は明確
- Supabase 直呼びはなく、データアクセスは `api.ts` を経由している
- `queryKey` 構造と mutation 後の `invalidate` は概ね適切
- 主要コンポーネントはアロー関数で記述され、Props も `interface` 定義で統一されている